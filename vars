# This is the example of vars file. Move it into .env/ directory

APP_SECRET=change_on_real_secret_key
DEBUG_MODE=True

POSTGRES_USER=choose_one_user
POSTGRES_PASSWORD=password
POSTGRES_DB=choose_one_db
DB_HOST=postgres
DB_PORT=5432

ACCESS_TOKEN_LIFETIME_HOURS=40
ACCESS_TOKEN_LIFETIME_MINUTES=0
REFRESH_TOKEN_LIFETIME_DAYS=7

API_VERSION=v0.1.0

EMAIL_USER=
EMAIL_PASSWORD=

BROKER_URL=redis://redis:6379

DJANGO_LOG_PATH=.data/django/choose_one.log
CELERY_LOG_PATH=.data/django/celery.log

HOST=

CORS_ORIGINS=http://0.0.0.0
CORS_ALLOW_ALL=False